1. Mason Rocha, Jasper Wilson, Calvin Mitchell
2. To describe an interface decleration, you would define a class with private or protected varaibles and one or more virtual functions that you can override.
3a. interface inheritance
3b. These should be private because we will not be inheriting
4. The test cases we ran were putting an empty file and a file full of chars. These test cases both worked. We got the expected values from our functions for write append and getSize.
5. We used AbstractFile* text = new TextFile("name"); // so we made a pointer of the abstract class to a textfile because this is how it inherts an abstract class.